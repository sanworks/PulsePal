Updates from r0.4:

-Reconfigured gound plane to reduce noise
-Eliminated 750-ohm resistors on output channels (acted as voltage divider with load; unnecessary)
-With same noise floor (~50mV p2p), system now provides 30mA/channel.