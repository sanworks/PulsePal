***Note*** 
The Python3 class does not use calls to obj.connect(PortName) and obj.disconnect()
Instead, initialize the object as P = PulsePalObject(PortName). 
Deleting the object will disconnect from the port automatically: del P
